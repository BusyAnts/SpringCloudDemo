server:
  port: 3377

spring:
  application:
    name: microservice-config-nacos-client
  cloud:
    nacos:
      discovery:
        #server-addr: localhost:8848  # Nacos服务注册中心
        server-addr: 192.168.20.90:1111
      config:
        #server-addr: localhost:8848  # Nacos作为配置中心地址
        server-addr: 192.168.20.90:1111
        file-extension: yaml  # 指定配置文件格式
